{
  "name": "technical-maintenance-assistant-cloudflare",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply maintenance-assistant-db --local",
    "db:remote": "wrangler d1 migrations apply maintenance-assistant-db --remote",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.29.0"
  },
  "engines": {
    "node": ">=20"
  }
}
